# CalmPaws 算法逻辑文档
## 服务器端推理 + 物理算法

---

## 一、整体数据流

```
ESP32 上传原始XYZ（100Hz，500行/次）
         ↓
  服务器 inference.py
  ① 降采样：100Hz → 40Hz（取第0,2,4...行）
  ② 取最近120行（3秒窗口 @40Hz）
  ③ DSP特征提取：120×3 → 39个特征
  ④ TFLite模型推理：39特征 → 4类概率
  ⑤ 置信度过滤：<55%时保留上一次标签
         ↓
      AI标签：calm / pacing / playing / shivering
         ↓
  服务器 algorithm.py
  ⑥ 物理算法：应激检测 + 翻身检测
  ⑦ 累加计数器：str_c/str_d/shiv_c/shiv_d/pace_d/play_d/roll_c
         ↓
      返回JSON给APP（字段与旧BLE完全兼容）
```

---

## 二、推理模块（inference.py）

### 2.1 基本参数

| 参数 | 值 | 说明 |
|------|-----|------|
| SOURCE_HZ | 100 | ESP32采样频率 |
| TARGET_HZ | 40 | 模型训练频率 |
| DOWNSAMPLE_STEP | 2 | 每2行取1行（100→40Hz）|
| WINDOW_ROWS | 120 | 推理窗口行数（3秒@40Hz）|
| LABELS | calm/pacing/playing/shivering | 4类行为 |
| UNCERTAIN_THRESHOLD | 0.55 | 置信度低于此值时保留上次标签 |
| FFT_LENGTH | 16 | FFT点数 |
| DO_OVERLAP | True | FFT重叠（步进=fft_points/2=8）|
| START_BIN | 1 | FFT起始bin（跳过直流分量）|
| STOP_BIN | 9 | FFT结束bin |
| NUM_BINS | 8 | 使用的FFT bin数量 |

### 2.2 降采样

```python
arr = np.array(raw_xyz)       # (500, 3) @100Hz
arr = arr[::2]                 # (250, 3) @40Hz
window = arr[-120:]            # 取最新120行
```

原理：每隔一行取一行，将100Hz降为40Hz，与Edge Impulse训练时的采样率对齐。

### 2.3 DSP特征提取（每轴13个特征 × 3轴 = 39个特征）

这是整个系统最核心的部分，完全复现了Edge Impulse C++ feature.hpp V4版本的计算逻辑。

#### 每轴处理步骤：

**Step 1：去均值**
```python
sig_zm = sig - mean(sig)
```

**Step 2：计算RMS（均方根）**
```python
rms = sqrt(mean(sig_zm²))
```

**Step 3：计算偏度（Skewness）**
```python
skewness = mean(sig_zm³) / stddev³
```
反映信号分布的不对称性。平静状态接近0，剧烈运动时绝对值增大。

**Step 4：计算峰度（Kurtosis，Fisher定义）**
```python
kurtosis = mean(sig_zm⁴) / stddev⁴ - 3
```
反映信号分布的尖峰程度。正态分布=0，冲击信号>0。

**Step 5：Welch Max-Hold FFT**
```python
# 步进8（overlap），取每个bin的最大功率谱值
fft_out = welch_max_hold(sig_zm, fft_points=16, do_overlap=True)
# 输出8个bin的功率谱
```
用滑动窗口FFT取最大值，比普通FFT更能捕捉瞬时频率特征。

**Step 6：FFT偏度和峰度**
```python
fft_skew = mean(fft_norm³)
fft_kurt = mean(fft_norm⁴) - 3
```
描述功率谱分布形状。

**Step 7：FFT bins log化（8个）**
```python
fft_log = log10(fft_out)   # 8个值
```
对数变换压缩动态范围，提升小信号区分度。

#### 13个特征汇总（每轴）：

| 序号 | 特征名 | 物理含义 |
|------|--------|---------|
| 1 | RMS | 信号能量（运动强度）|
| 2 | Skewness | 时域分布不对称性 |
| 3 | Kurtosis | 时域冲击特性 |
| 4 | FFT_Skewness | 频谱分布不对称性 |
| 5 | FFT_Kurtosis | 频谱冲击特性 |
| 6~13 | FFT bins [1~8] | 各频段能量（log10）|

#### 三轴特征排列：
```
[X轴13个特征, Y轴13个特征, Z轴13个特征] = 39个特征
```

### 2.4 TFLite模型推理

```python
input:  (1, 39)   float32 特征向量
output: (1, 4)    float32 概率 [calm, pacing, playing, shivering]
```

模型文件：`model.tflite`（~10KB，来自Edge Impulse项目968791）
训练准确率：90.31%

### 2.5 置信度过滤

```python
if best_prob < 0.55:
    final_label = last_label    # 保留上一次标签
else:
    final_label = best_label    # 使用新标签
    last_label  = best_label    # 更新记忆
```

作用：避免短暂噪声导致标签频繁跳变，提升稳定性。

### 2.6 返回格式

```json
{
  "label":        "calm",        // 最终标签（含平滑）
  "confidence":   0.8024,        // 最高类别置信度
  "probs": {
    "calm":       0.8024,
    "pacing":     0.0936,
    "playing":    0.0694,
    "shivering":  0.0387
  },
  "is_uncertain": false,         // 是否低置信度（<55%）
  "raw_label":   "calm"          // 原始预测标签（未平滑）
}
```

---

## 三、物理算法模块（algorithm.py）

### 3.1 应激检测（宏颤抖算法）

**逻辑来源**：复现旧ESP32 C++固件的宏颤抖检测

**检测条件**（在一个5秒窗口内）：
```python
G值在 1.2~2.2 之间（中等力度运动）
AND 相邻采样G值差 > 0.3（有节律性抖动）
AND 最大G < 4.0（排除剧烈碰撞）
AND 满足以上条件的采样点 >= 2个
```

**计算G值**：
```python
G = sqrt(x² + y² + z²) / 9.80665   # 单位：g（重力加速度倍数）
```

**触发时**：
- str_c（应激次数）+1
- str_d（应激持续秒数）+= 本次窗口时长（通常5秒）

### 3.2 翻身检测（Z轴极性反转）

```python
if z_first > 0.6 and z_last < -0.6:
    roll_c += 1
```

检测窗口内Z轴从正向（>0.6g，背部朝上）变为负向（<-0.6g，腹部朝上），判定为翻身一次。

### 3.3 AI标签累计

| AI标签 | 累计字段 | 逻辑 |
|--------|---------|------|
| shivering | shiv_c +1, shiv_d += 5s | 每次识别到发抖，次数+1，持续时间+5秒 |
| pacing | pace_d += 5s | 踱步只记录持续时间，不记次数 |
| playing | play_d += 5s | 玩耍只记录持续时间，不记次数 |
| calm | 无累计 | 平静是兜底状态，不记录 |

### 3.4 输出字段（与旧BLE JSON完全兼容）

| 字段 | 类型 | 说明 |
|------|------|------|
| timestamp | long | 毫秒级Unix时间戳 |
| str_c | int | 应激次数（当日累计）|
| str_d | float | 应激持续秒数（当日累计）|
| shiv_c | int | 发抖次数（当日累计）|
| shiv_d | float | 发抖持续秒数（当日累计）|
| pace_d | float | 踱步持续秒数（当日累计）|
| play_d | float | 玩耍持续秒数（当日累计）|
| roll_c | int | 翻身次数（当日累计）|
| battery | int | 电量百分比（来自ESP32心跳）|
| rssi | int | 信号强度（固定-60，BLE时代遗留字段）|

---

## 四、服务器状态管理（server.py）

### 4.1 设备状态结构

每台设备在内存中独立维护一套状态：

```python
devices[device_id] = {
    'inference':       PetInference实例,   # TFLite推理器
    'algo_state':      AlgorithmState实例, # 当日累计计数器
    'buffer':          [],                 # 原始XYZ滚动缓冲（最近600行）
    'last_packet':     None,               # 最新一次输出JSON
    'last_battery':    100,                # 最新电量
    'app_last_seen':   0,                  # APP最近请求时间戳
    'upload_interval': 60,                 # 当前下发给ESP32的上传间隔（秒）
}
```

### 4.2 实时/后台模式判断

```python
def is_app_online(dev):
    return (time.time() - dev['app_last_seen']) < 15
```

APP在15秒内有过请求 → 实时模式（interval=5）
否则 → 后台模式（interval=60）

### 4.3 XYZ缓冲管理

```python
dev['buffer'].extend(samples)     # 追加新数据
if len(buffer) > 600:
    buffer = buffer[-600:]        # 保留最近600行（约6秒）

if len(buffer) >= 300:            # 至少3秒才推理
    result = inference.predict(buffer)
```

---

## 五、4类行为特征（供调试参考）

| 行为 | RMS特征 | 频率特征 | 典型场景 |
|------|--------|---------|---------|
| calm | 低 | 低频为主 | 趴着休息、睡觉 |
| pacing | 中 | 规律节律 | 来回走动、焦虑踱步 |
| playing | 高 | 不规律宽频 | 奔跑、玩耍、扑咬 |
| shivering | 低~中 | 高频小幅振动 | 发抖、寒颤、恐惧颤抖 |

---

## 六、模型信息

| 参数 | 值 |
|------|-----|
| 项目 | Edge Impulse petoteco #968791 |
| 模型文件 | model.tflite |
| 文件大小 | ~10KB |
| 输入形状 | (1, 39) float32 |
| 输出形状 | (1, 4) float32 |
| 训练准确率 | 90.31% |
| DSP版本 | implementationVersion=4 |
| 采样频率 | 40Hz |
| 窗口大小 | 3000ms（120行）|

---

## 七、算法设计参考文献

### 7.1 发抖检测（Shiver）— 文献依据

发抖检测由 AI 模型完成，模型学习的信号特征（高频4–16Hz、低幅、FFT高频bin能量显著）有以下文献支持：

| 文献 | 作者/来源 | 年份 | 关联内容 |
|------|----------|------|---------|
| *Postural tremor analysis in dogs and cats using accelerometry and video-based applications* | ResearchGate | 2024 | **直接依据**：加速度计实测犬猫姿势性震颤，验证频率范围和g值幅度，与本系统特征提取方法（Welch FFT）相同 |
| *Primary orthostatic tremor and orthostatic tremor-plus in dogs: 60 cases (2003–2020)* | J Vet Intern Med | 2022 | 站立时高频震颤13–18 Hz，支持高频区间阈值设定 |
| *Recent Advances in Tremors in Dogs and Cats* | ResearchGate | 2024 | 综述：生理性颤抖（寒冷/恐惧）频率范围4–16 Hz，幅度小 |
| *Generalized Tremors in Dogs: 198 Cases (2003–2023)* | PMC 11951301 | 2025 | 大样本（n=198）震颤临床特征，覆盖疼痛、低体温、恐惧诱发的发抖信号特征 |
| *Development and Validation of an IMU Sensor-Based Behaviour-Alert Detection Collar for Assistance Dogs* | PMC 12607429 | 2026 | IMU 项圈行为检测，验证加速度计在犬类行为分类中的可行性 |
| **Edge Impulse petoteco #968791（本项目）** | 项目训练数据 | 2024 | **直接依据**：用实际发抖数据训练，准确率 90.31% |

### 7.2 踱步检测（Pacing）— 文献依据

踱步信号特征（低频0.5–2Hz规律性摆动）由 AI 模型识别，训练数据来源：

| 来源 | 说明 |
|------|------|
| **Kaggle DogMoveData** | 公开宠物运动数据集，含纯踱步（Pacing）标签，已清洗：排除 Walking / Trotting，保留约771秒（200窗口）|
| *Evaluating Data Preprocessing Strategies for Canine Activity Recognition Using Wearable IMUs* | ACM, 2025 | 讨论 IMU 数据预处理对犬类活动分类的影响，支持 100Hz→40Hz 降采样策略 |

### 7.3 应激检测（Stress）— 算法来源

| 来源 | 说明 |
|------|------|
| **旧 ESP32 C++ 固件** | 宏颤抖算法（G值1.2–2.2，相邻差>0.3）是从旧固件直接复现至 `algorithm.py`，保持行为定义一致性 |
| *Remote Vital Sensing in Clinical Veterinary Medicine* | MDPI Animals, 2025 | 综述可穿戴传感器在兽医领域的应用，支持 G 值阈值判断动物应激状态 |

---

## 七、计数重置

每天0点，APP调用：
```
POST /api/reset/{device_id}
```
服务器重置所有累计计数器（str_c, str_d, shiv_c...等归零）。

